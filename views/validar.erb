<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>🔍 Validar Token</title>
  <style>
    :root {
      --bg: #ffffff;
      --text: #2d3748;
      --border: #e2e8f0;
      --primary: #3182ce;
      --success: #48bb78;
      --danger: #e53e3e;
      --light-bg: #f8fafc;
      --card-bg: #ffffff;
    }

    @media (prefers-color-scheme: dark) {
      :root {
        --bg: #1a202c;
        --text: #e2e8f0;
        --border: #4a5568;
        --light-bg: #2d3748;
        --card-bg: #2d3748;
      }
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      background-color: var(--bg);
      color: var(--text);
      line-height: 1.6;
      padding: 20px;
    }

    .container {
      max-width: 700px;
      margin: 0 auto;
    }

    header {
      text-align: center;
      margin-bottom: 2rem;
    }

    h1 {
      font-size: 2rem;
      margin-bottom: 0.5rem;
      color: var(--primary);
    }

    .subtitle {
      color: var(--text);
      opacity: 0.8;
    }

    .card {
      background: var(--card-bg);
      border: 1px solid var(--border);
      border-radius: 12px;
      padding: 24px;
      margin-bottom: 24px;
      box-shadow: 0 4px 6px rgba(0,0,0,0.05);
    }

    .form-group {
      margin-bottom: 16px;
    }

    label {
      display: block;
      margin-bottom: 6px;
      font-weight: 600;
    }

    textarea {
      width: 100%;
      padding: 12px;
      border: 1px solid var(--border);
      border-radius: 8px;
      background: var(--light-bg);
      color: var(--text);
      font-family: monospace;
      font-size: 14px;
      height: 100px;
    }

    button {
      background: var(--primary);
      color: white;
      border: none;
      padding: 12px 24px;
      border-radius: 8px;
      font-size: 16px;
      cursor: pointer;
      font-weight: 600;
      width: 100%;
      transition: opacity 0.2s;
    }

    button:hover {
      opacity: 0.9;
    }

    .alert {
      padding: 16px;
      border-radius: 8px;
      margin: 16px 0;
    }

    .alert-error {
      background: #fee;
      border: 1px solid var(--danger);
      color: var(--danger);
    }

    .alert-success {
      background: #eff;
      border: 1px solid var(--success);
      color: var(--success);
    }

    .result ul {
      list-style: none;
      margin-top: 12px;
    }

    .result li {
      padding: 8px 0;
      border-bottom: 1px dashed var(--border);
    }

    .result li:last-child {
      border-bottom: none;
    }

    strong {
      font-weight: 600;
    }

    footer {
      text-align: center;
      margin-top: 32px;
      font-size: 0.9rem;
      opacity: 0.7;
    }

    a {
      color: var(--primary);
      text-decoration: none;
    }

    a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="container">
    <header>
      <h1>🔍 Validador de Token</h1>
      <p class="subtitle">Cole um token gerado para verificar sua autenticidade e validade</p>
    </header>

    <main>
      <div class="card">
        <form method="post" action="/validar">
          <div class="form-group">
            <label for="token">Token Assinado</label>
            <textarea id="token" name="token" placeholder="eyJjcGYiOiIwMDAuMDAwLjAwMC0wMCIsIm5vbWUiOiJKb...:a1b2c3d4..."><%= @token_input || '' %></textarea>
          </div>
          <button type="submit">Validar Token</button>
        </form>
      </div>

      <% if @erro %>
        <div class="alert alert-error">
          <strong>❌ Token inválido ou expirado!</strong><br>
          <%= @erro %>
        </div>
      <% elsif @payload %>
        <div class="card alert alert-success">
          <h2>✅ Token Válido!</h2>
          <div class="result">
            <ul>
              <% @payload.each do |k, v| %>
                <li>
                  <strong>
                    <%= k.to_s.capitalize %>:</strong>
                  <% if k == :exp %>
                    <%= Time.at(v).strftime("%d/%m/%Y %H:%M:%S") %>
                  <% else %>
                    <%= v %>
                  <% end %>
                </li>
              <% end %>
            </ul>
          </div>
        </div>
      <% end %>
    </main>

    <footer>
      <p><a href="/">« Gerar um novo token</a></p>
    </footer>
  </div>
</body>
</html>